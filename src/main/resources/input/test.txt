#+ TEST PARA VER GUARDADO MIXTO EN TABLA DE SIMBOLOS +#
#+ Objetivo: NO declarar todo en init para verificar altas por uso +#

init {
    contador,limite,paso,contador4,a,b,c,d : Int
    mensaje : String
    #+ identificadorQueSuperaCuarentaCaracteresFalla : String +# 
    #+ En caso de descomentar linea 7 falla por longiut del identificador +# 
    mensaje2 : Int #+ --- 'mensaje' Lanzar Error de "re-declaracion" EN INIT --- +#
    precio , volumen: Float
    resultado :Int #+ --- en caso de eliminar/comentar salta excepcion personalizada para pedir su decllaracion en caso de no poder inferir. EJ:textoSalida, pesoInferido" EN INIT --- +#
    puntoflotante1 : Float
    puntoflotante2 : Float
    puntoflotante3 : Float
    puntoflotante4 : Float
}

#+ --- DECLARADAS EN INIT --- +#
contador = 1
limite = 3
mensaje = "LIT INICIO"
print(mensaje)
puntoflotante1 = 9999.99 
puntoflotante2 = 99.
puntoflotante3 = .9999
contadorNegativo = -10
puntoflotanteNegativo = -.9999

contador4 = 32767 #+ En caso de 32768 falla por tamaño del integer (16 bits)+# 
puntoflotante4 = 65504.0 #+ En caso de 65505 falla por tamaño del float (16 bits)+# 

#+ --- NO DECLARADAS (deben aparecer por uso/asignacion) --- +#
resultado = 21 / 4
resultado = 27 - resultado
resultado = 34 * 3
textoSalida = "LIT STR ASIGN"
pesoInferido = 35


#+ --- IF --- +#
if (contador > limite)
{
    print("LIT_IF_MAYOR SIN ELSE")
}


#+ --- IF / ELSE --- +#
if (contador > limite)
{
    print("LIT_IF_MAYOR")
}
else
{
    print("LIT_IF_MENOR_IGUAL")
}

#+ --- IF ANIDADO CON ELSE --- +#
if (contador > limite)
{
    print("LIT_IF_MAYOR")
}
else if (contador + 1 > limite)
{
    print("IF anidado")
}
else
{
    print("else del IF anidado")
}
#+ --- IF ANIDADO DENTRO DE OTRO IF --- +#
if (contador > limite)
{
    if (contador + 2 > limite)
    {
        print("LIT_IF_MAYOR + 2")
    }
    print("LIT_IF_MAYOR")
}

#+ --- WHILE CON CONDICION CLASICA --- +#
while (contador > limite)
{
    print("contador mayor que limite")
    contador = contador + 1
}

#+ --- WHILE ESPECIAL IN [ ... ] --- +#

while (itemListaA in [1,2,3])
{
   print(itemListaA)
}

while (itemListaB in [1+1,2*2,3/3])
{
   print(itemListaB)
}

#+ --- READ --- +#
read(limite)

#+ --- FOR (variables por uso) --- +#
FOR i = 1 TO 10 STEP 2
   print(i)
NEXT i

FOR i = 1 TO 5
   print(i)
NEXT i 

#+ --- CONDICIONALES --- +#

if (a > b)
{
    print(" A mayor a B")
}

if (a > b AND c > d)
{
    print(" A mayor a B Y C mayor a D")
}


if (a > b OR c > d)
{
    print(" A mayor a B O C mayor a D")
}



while(a < b AND c >d)
{
    print(" Mientras A menor B Y C mayor a D")
}


#+ --- READ Y PRINT - ERROR VARIABLE NO DECLARADA --- +#
#+ read(variableInexistente) +#
#+ print(variableInexistente) +#

#+ --- CASOS PARA PROBAR ERROR SEMÁNTICO EN ASIGNACION (id con id) --- +#
#+ precio = contador +#  #+ Debería fallar: FLOAT = INT +#
#+ contador = precio +#  #+ Debería fallar: INT = FLOAT +#
#+ mensaje = contador +# #+ Debería fallar: STRING = INT +#

#+ --- CASOS PARA PROBAR ERROR SEMÁNTICO EN ASIGNACION (id con cte) --- +#
#+ precio = "contador" +#  #+ Debería fallar: FLOAT = STRING +#
#+ precio = 10 +#  #+ Debería fallar: FLOAT = INT +#
#+ mensaje = 10 +# #+ Debería fallar: STRING = INT +#

#+ --- CASOS PARA PROBAR Validación de ID no declarada pero usada --- +#
#+ if (NOT e){    print(" Not E")} +#

#+ --- CASOS PARA PROBAR Validación de igual tipo en condiciones --- +#
#+if( mensaje> contador){    print("esta mal la validacion de igual tipo en condiciones")}+#

#+ --- CASOS PARA PROBAR Validación de igual tipo en FOR --- +#
#+ FOR precio = 1 TO 5   print(i)NEXT i +#